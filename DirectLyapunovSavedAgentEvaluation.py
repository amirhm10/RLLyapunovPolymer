"""Run saved-agent evaluation from the repository root.

The implementation lives in ``Simulation.saved_agent_evaluation`` so this root
entrypoint stays consistent with the other experiment scripts.
"""

from Simulation.saved_agent_evaluation import main


if __name__ == "__main__":
    main()
